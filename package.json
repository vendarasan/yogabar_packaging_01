{
  "name": "yogabar-packaging",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server/index.js",
    "build": "npm run build --prefix client",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "test": "npm test --prefix server"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
