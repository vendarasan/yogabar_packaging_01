const React = require('react');
const ReactDOMServer = require('react-dom/server');

// To run JSX or ESM in Node, we can use esbuild or vite or test individual logic
console.log('Testing server-side render...');
